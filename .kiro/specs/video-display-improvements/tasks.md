# 実装タスク

## はじめに

このドキュメントは、`video-display-improvements`機能の実装タスクを定義します。各タスクは、要件ドキュメント（requirements.md）および技術設計ドキュメント（design.md）に基づいて構成されています。

## 進捗状況（2025-10-30 更新）

**実装済み**: タスク1〜6（✅）
**進行中**: タスク7（🔄）- 手動検証とデプロイ準備
**未着手**: なし

### 主要な実装成果
- ✅ TDD方式で動画詳細コンポーネントの表示ロジックを修正（全25テストパス）
- ✅ 28個の動画メタデータファイルを一括修正（タグ、記号、URL、関連動画）
- ✅ 動画詳細ページにYouTubeリンクを追加
- ✅ TypeScript型チェック: エラーなし
- ✅ テスト実行: 1046/1049テスト成功（3個スキップ、失敗ゼロ）
- ✅ 本番ビルド: 成功（28個の動画ページを静的生成）

### 残作業
1. **手動検証**（推定15分）:
   - ブラウザでの表示確認
   - YouTube概要欄へのコピーペーストテスト
   - リンク動作確認

## タスク一覧

- [x] 1. データ修正準備とバックアップ
- [x] 1.1 動画メタデータファイルのバックアップと修正対象の特定
  - すべての動画メタデータファイル（`src/data/videos/*.ts`）を確認
  - タグに`#`記号が含まれているファイルをリストアップ
  - Udemy講座URLに不正なパス（`/courses`、`/cta`等）が含まれるファイルをリストアップ
  - 半角記号（特に感嘆符`!`）が含まれるファイルをリストアップ
  - 存在しない動画に対応するメタデータファイルを特定
  - _Requirements: 9.1, 9.2_

- [x] 2. 動画メタデータファイルの修正
- [x] 2.1 タグデータの`#`記号を削除
  - すべての動画メタデータファイルのタグ配列から`#`記号を削除
  - タグデータが純粋な文字列配列となることを確認
  - 削除後のデータがTypeScript型定義に準拠していることを検証
  - _Requirements: 3.1, 3.2_

- [x] 2.2 記号を全角に統一
  - すべての動画メタデータファイル内の半角感嘆符（`!`）を全角（`!`）に変換
  - タイトル、セクション説明、箇条書き項目など全てのテキストで記号の統一を確認
  - 日本語コンテンツとして自然な表示となることを検証
  - _Requirements: 4.1, 4.2, 4.3_

- [x] 2.3 箇条書きマーカーを中点「・」に統一
  - 関連動画のタイトルから絵文字を削除し、中点「・」に置き換え
  - セクションタイトルには絵文字を保持
  - 箇条書き項目が一貫した視覚的体験を提供することを確認
  - _Requirements: 7.1, 7.2, 7.3_

- [x] 2.4 Udemy講座URLを`/coupons`形式に修正
  - すべてのUdemy講座URLを`https://www.vibecodingstudio.dev/coupons`に統一
  - Claude Code関連動画には`?topic=claude-code`パラメータを追加
  - Codex関連動画には`?topic=codex`パラメータを追加
  - `/courses`や`/cta`などの不正なパスを正しい`/coupons`パスに修正
  - すべてのURLが実際に存在し、アクセス可能であることを確認
  - _Requirements: 10.1, 10.2, 10.3, 10.4, 10.5_

- [x] 2.5 `/coupons`ページとの整合性を確保
  - 動画メタデータ内のUdemy講座情報が`/coupons`ページの情報と一致することを確認
  - `/coupons`ページに存在しない講座への参照を削除または修正
  - 動画トピックに関連する講座フィルタリング機能を活用したURLを設定
  - _Requirements: 11.1, 11.2, 11.3_

- [x] 2.6 概要セクションの改行を最適化
  - 概要セクションの長いテキストに適切な位置で改行を挿入
  - 複数の文が含まれる場合は意味のまとまりごとに改行を配置
  - 冒頭セクション（opening.lines）の各行が独立した段落として表示されることを確認
  - _Requirements: 5.1, 5.2, 5.3_

- [x] 3. 不要な動画メタデータファイルの削除
- [x] 3.1 存在しない動画のメタデータファイルを削除
  - 対応するYouTube動画が存在しないメタデータファイルを削除
  - `src/lib/videos/video-data.ts`から削除したファイルのインポート文を除外
  - すべての動画IDが有効であることを確認
  - **結果**: 全28ファイルが有効と確認、削除不要
  - _Requirements: 9.1, 9.2, 9.3_

- [x] 4. 動画詳細コンポーネントの表示ロジック修正
- [x] 4.1 セクション区切り線の長さを短縮
  - `video-detail.tsx`の`formatVideoAsPlainText`関数内の`SECTION_DIVIDER`定数を修正
  - 区切り線の文字数を現在の長さ（約50文字）から30文字程度に短縮
  - モバイル表示でも視覚的に適切な長さとなることを確認
  - _Requirements: 1.1, 1.2_

- [x] 4.2 タグセクション前の区切り線を削除
  - タグセクションを最後のセクションとして表示するロジックを実装
  - タグセクション直前の区切り線を出力しないように修正
  - タグセクションが視覚的に自然に配置されることを確認
  - _Requirements: 1.3, 3.3_

- [x] 4.3 タグ表示時に`#`記号を自動付与
  - タグを表示する際に、各タグの先頭に`#`記号を自動的に付与するロジックを実装
  - タグデータには`#`記号が含まれていないことを前提とする
  - YouTube概要欄でのタグ機能が正しく動作することを確認
  - _Requirements: 3.1, 3.2, 3.3_

- [x] 4.4 「コピー用」などの注釈テキストを削除
  - プレーンテキスト出力から注釈的な要素を除外
  - 純粋な動画情報のみをフラットな形式で表示
  - YouTube概要欄にコピーペーストする際に不要なテキストが含まれないことを確認
  - _Requirements: 2.1, 2.2, 2.3_

- [x] 4.5 関連動画の表示形式を改善
  - 関連動画のタイトルとURLを1つの改行で区切って表示
  - 複数の関連動画が連続する場合、各動画の間に1行の空白行を挿入
  - タイトルとURLのセットが視覚的に明確に区別できることを確認
  - _Requirements: 6.1, 6.2, 6.3_

- [x] 5. 動画詳細ページへのYouTube動画リンク追加
- [x] 5.1 動画詳細ページにYouTubeリンクを追加
  - `src/app/videos/[id]/page.tsx`にYouTube動画へのリンクを表示
  - リンクは動画タイトル付近に視認性の高い形式で配置（赤いYouTubeボタン）
  - `target="_blank"`および`rel="noopener noreferrer"`属性を設定して新しいタブで開く
  - _Requirements: 8.1, 8.2, 8.3_

- [x] 6. テストと検証 **（✅ 完了）**
- [x] 6.1 単体テストの実装と実行
  - `formatVideoAsPlainText`関数のテストを実装 ✅
  - タグに`#`記号が自動付与されることを検証するテストケース ✅
  - セクション区切り線の長さが短縮されていることを検証するテストケース ✅
  - タグセクション前の区切り線が存在しないことを検証するテストケース ✅
  - 関連動画の表示形式が改善されていることを検証するテストケース（タイトルとURLの改行、動画間の空白行）✅
  - **結果**: video-detail.test.tsx - 全25テスト成功
  - _Requirements: すべての要件に対応_

- [x] 6.2 動画メタデータの整合性テスト
  - すべての動画データファイルでタグに`#`記号が含まれていないことを検証 ✅
  - Udemy講座URLが`https://www.vibecodingstudio.dev/coupons`形式であることを検証 ✅
  - 記号が全角であることを検証（特に感嘆符`!`）✅
  - 箇条書きマーカーが中点「・」で統一されていることを検証 ✅
  - **結果**: データ整合性テスト成功
  - _Requirements: 3.1, 4.1, 7.1, 10.1_

- [x] 6.3 統合テストの実装と実行
  - 動画詳細ページで動画データが正しくプレーンテキスト形式で表示されることを検証 ✅
  - YouTube動画リンクが正しく表示されることを検証 ✅
  - 存在しない動画IDで404ページが表示されることを検証 ✅
  - 削除された動画ファイルがデータローダーから除外されていることを検証 N/A（削除なし）
  - **結果**: すべての統合テスト成功
  - _Requirements: 8.1, 9.3_

- [x] 6.4 型チェックとビルドの確認
  - TypeScript型チェック（`npm run type-check`）を実行してエラーがないことを確認 ✅
  - 本番ビルド（`npm run build`）が成功することを確認 ✅
  - ビルドサイズが許容範囲内であることを確認 ✅
  - **結果**: 型チェック成功、テスト1046/1049成功（3個スキップ）、ビルド成功
  - _Requirements: すべての要件に対応_

- [ ] 7. 手動検証とデプロイ準備
- [ ] 7.1 プレビュー環境での手動検証
  - 複数の動画詳細ページを開き、表示が正しいことを確認
  - プレーンテキスト形式の内容をクリップボードにコピーし、YouTube概要欄に正しく貼り付けられることを確認
  - Udemy講座リンクをクリックして、正しく`/coupons`ページに遷移することを確認
  - YouTube動画リンクをクリックして、新しいタブで動画ページが開くことを確認
  - _Requirements: すべての要件に対応_

- [ ] 7.2 ロールバック計画の確認
  - Gitでのロールバック手順を確認
  - 問題が発生した際の修正プロセスを明確化
  - デプロイ後の監視ポイントをリストアップ
  - _Requirements: なし（運用要件）_

## タスクの実行順序

上記のタスクは、番号順に実行することを推奨します。各タスクは前のタスクに依存しているため、順序を守ることで整合性が保たれます。

## 実装完了の基準

すべてのタスクが完了し、以下の条件を満たした時点で実装完了とします：

1. ✅ すべての単体テストおよび統合テストが合格（1046/1049成功、3個スキップ）
2. ✅ TypeScript型チェックでエラーがない
3. ✅ 本番ビルドが成功
4. ⏳ プレビュー環境での手動検証が完了
5. ⏳ YouTube概要欄へのコピーペーストが正しく機能
6. ⏳ Udemy講座リンクが正しく動作

## 注意事項

- 各タスクの完了後は必ずコミットを行い、段階的に進めることを推奨します
- 問題が発生した場合は、速やかにロールバックして修正を行います
- テストが失敗した場合は、原因を特定して修正した後に再度テストを実行します
